# Create the real secret out of band, never commit it:
#   kubectl -n summit create secret generic summit-management \
#     --from-literal=token="$(openssl rand -hex 32)"
apiVersion: v1
kind: Secret
metadata:
  name: summit-management
  namespace: summit
type: Opaque
stringData:
  token: replace-me
